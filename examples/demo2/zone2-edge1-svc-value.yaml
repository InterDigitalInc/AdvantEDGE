# Default values for generic values.yaml
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

name: meep-demo2-zone2-edge1-svc

deployment:
  name: zone2-edge1-svc
 
service:
  name: azone2-edge1-svc
