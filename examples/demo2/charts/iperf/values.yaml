# Default values for generic values.yaml
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

namespace: default
serviceaccount: meep-user

deployment:
  name: iperf
  image:
    name: gophernet/iperf-server
    pullpolicy: IfNotPresent
