# Default values for generic values.yaml
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

namespace: default

deployment:
  image:
    name: advantedge-master.com:5000/demo-server
    pullpolicy: IfNotPresent

service:
  name: ""
  port: 80
  protocol: TCP
  group: svc
